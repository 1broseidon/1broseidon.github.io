{
  "name": "chain.sh",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "dev": "node build.mjs && wrangler dev",
    "deploy": "npm run build && wrangler deploy",
    "repos": "node scripts/fetch-repos.mjs",
    "prebuild": "node scripts/fetch-repos.mjs"
  },
  "devDependencies": {
    "wrangler": "^4.42.0"
  }
}
